# Release 目录约束

英语 `en-US` 是主语言。

修改英语上架内容后，必须同步更新 `zh-Hans`、`ja` 和 `ko` 的对应内容。

所有语言的 `metadata.md` 必须保持相同的信息结构，包括 App 标题、副标题、推广文本、描述、关键词、新版本说明、支持信息和资源目录说明。

新增语言时，必须复制当前主语言结构，并补齐本地化初始文本。

不要只更新单一语言的字段结构。
